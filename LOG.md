# 工作日志规范（Work Log）

> 配套 `PROCESS.md`。一份**连续的时间线日志**，记录策划/开发阶段每天做了什么、为什么、结果如何。
> 用途：追溯决策上下文、回顾进展、跨会话续接、复盘（"上周卡在哪"）。

## 工作日志 vs 其他文档的分工

| 文档 | 粒度 | 回答 | 何时写 |
|---|---|---|---|
| `WORKLOG.md`（本日志） | 每次会话/每天 | **今天做了什么、结果如何** | 每次工作结束时 |
| `DESIGN.md` | 章节 | **设计是什么**（当前状态） | 阶段推进时 |
| `confirmations/` | 阶段 | **本阶段确认了什么** | 每阶段确认门 |
| `adr/` | 决策 | **重大决策为什么这么定** | 重大决策时 |
| `git log` | 提交 | **代码/文档变更记录** | 每次 commit |

> 关系：日志是"叙事线"，DESIGN.md 是"快照"，ADR 是"决策档案"，git 是"变更档案"。
> 查"当时为什么这么做" → 日志+ADR；查"现在设计是什么" → DESIGN.md；查"改了什么" → git log。

## 文件位置与格式

```
<游戏>/docs/WORKLOG.md
```

```markdown
# <游戏名> — 工作日志

> 规则：每次工作结束追加一条（倒序，最新在上）。与 git commit 同步进行。

## 2026-08-18（第 1 次会话）
- 阶段：阶段 0-3 设计
- 做了什么：
  - 完成阶段 0 执行摘要（修仙肉鸽卡牌，事件叙事卖点）
  - 完成阶段 1 核心循环（三级循环，无固定Boss）
  - 完成阶段 2 三大支柱（回合制/分层秘境/五件套）
  - 完成阶段 3 功能清单与 MVP（12 功能，完整单局闭环）
- 关键决策：<无固定Boss用随机大事件>（见 adr/）
- 验证/结果：<确认稿已提交，待用户确认>
- 卡点/未决：<待确认：事件文本风格、正式名称>
- 下一步：<审阅确认稿 → 阶段 4 数值>

## 2026-08-19（第 2 次会话）
- 阶段：阶段 4 数值
- 做了什么：…
- …
```

## 规则

1. **每次工作结束必写**：一条日志 = 一次会话/一天的工作，与最后的 git commit 同步
2. **倒序**：最新记录在最上，方便一眼看到最近进展
3. **必含字段**：日期 / 阶段 / 做了什么 / 关键决策 / 验证结果 / 卡点未决 / 下一步
4. **决策要链接**：重大决策在日志里引用 ADR 编号，不重复写推理
5. **诚实记录卡点**："卡点/未决"字段必填——这是复盘和续接的入口
6. **开发期同样适用**：进入开发后，日志记录每个功能的开发/测试/提交
7. **跨会话续接**：新会话读日志第一条 = 上次进展与下一步

## 与确认门的关系

- 每阶段确认稿提交 → 日志记一条"确认稿待确认"
- 用户确认 → 日志更新该条状态
- 阶段推进 → 日志记"进入阶段 X"

## 示例：从创建到开发的一条完整日志

```
## 2026-08-18（第 1 次会话）
- 阶段：阶段 0-3 设计
- 做了什么：
  - 搭建项目骨架（DESIGN.md 20 节母版 + adr/ + confirmations/）
  - 完成阶段 0-3 全部设计问答
- 关键决策：无固定Boss用随机大事件（adr/0001）
- 验证/结果：设计文档 20 节草稿完成；确认稿 4 份已提交
- 卡点/未决：事件文本风格未定；正式名称未定
- 下一步：用户审阅确认稿 → 阶段 4 数值
```
