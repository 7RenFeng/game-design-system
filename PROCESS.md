# 协作流程（Game Design Process）

> 这是 AI 与人协作做策划的"工作手册"。设计文档管"是什么"，本文件管"怎么一起做"。
> 每个新游戏项目：先建 `docs/DESIGN.md`（复制 TEMPLATE），再按本流程推进。

---

## 1. 角色分工（边界必须清晰）

| 事项 | 谁决定 | 说明 |
|------|--------|------|
| 玩法方向、类型、题材 | **人** | AI 只给选项和利弊，绝不替人拍板 |
| 功能清单、MVP 范围 | 人 定方向，AI 拟稿 | AI 出草案，人确认/修改 |
| 数值初始值 | AI 拟稿，原型期人调 | 文档只记初始值，手感以人试玩为准 |
| 代码、场景、脚本实现 | **AI** | 按设计文档规格实现 |
| 手感、画面、音效体验 | **人** | AI 判断不可靠，以人反馈为准 |
| 验收是否通过 | 人（AI 先自检） | AI 按验收标准自检，人最后拍板 |

**一句话**：AI 负责"把设计变成可执行的东西"，人负责"判断这个东西对不对"。

## 2. 阶段推进（顺序固定，每阶段一小步）

```
阶段0 概念 → 阶段1 核心循环 → 阶段2 三大支柱 → 阶段3 功能/MVP
        → 阶段4 数值 → 阶段5 内容 → 阶段6 风险 → 定稿 → 进入开发
```

### 每阶段的执行规则
1. AI 一次只问 **1~3 个问题**（选项式优先：给 2~3 个例子 + "或你自己说"）
2. 用户答完 → AI **立即写入 DESIGN.md** 对应小节（不等全部答完）
3. 用户说"没想法/随便/你定" → AI 给 3 个**具体选项**让用户挑，不给开放题
4. 每阶段结束 → 更新"设计进度"小节 + `git commit`（`docs: 设计-阶段X xxx`）
5. 核心循环未定 → **禁止前进**到支柱/功能阶段

### 收敛规则
- 用户跑题 → AI 温和拉回当前阶段
- 用户不确定 → 先给"安全默认"（如极简几何占位美术），后续可换
- 用户想法变了 → 直接改文档对应章节，commit 记录演变（`docs: 更新设计-xxx`）

## 3. 设计评审（每个里程碑前的自检清单）

进入下一里程碑（M0→M1→M2→M3）前，逐条打勾，有 ❌ 就先补：

- [ ] **循环完整**：核心循环能从"起点"走到"回到起点"，玩家有明确目标
- [ ] **规格可执行**：AI 读完功能清单能直接动手，每项 P0 有验收标准
- [ ] **MVP 收敛**：MVP 是 3~5 个功能的可玩闭环，没有"顺手加的"功能
- [ ] **数值有初始值**：所有玩法参数在文档里有初始值和调优目标
- [ ] **内容量级合理**：内容规划写清数量，不会导致工作量爆掉
- [ ] **风险有缓解**：每个高风险项有明确的缓解动作
- [ ] **决策有记录**：本里程碑期间的重大决策都写了 ADR

## 4. 决策记录（ADR，重大决策不靠记忆）

> ADR = Architecture/Design Decision Record。专业团队惯例：重大决策留痕。

### 触发条件（满足其一就写 ADR）
- 改变了设计支柱 / 核心循环 / MVP 范围
- 换了引擎、架构、技术路线
- 数值/手感方向大改（如"从轻盈改厚重"）
- 砍掉或新增了 P0 功能

### 写法
复制 `adr/_template.md` → 命名 `adr/000N-一句话主题.md`（编号递增）→ 填写 → commit
并在 DESIGN.md 的"设计决策记录"索引表加一行。

### ADR 的内容（5 问）
1. 背景：为什么现在要做这个决策？
2. 选项：考虑过哪几个方案（至少 2 个）？
3. 决策：选了哪个，为什么？
4. 后果：选它带来什么好处 / 什么代价？
5. 状态：已接受 / 已替代（由哪个 ADR 替代）

## 5. 跨会话续接（换电脑 / 新对话 / 上下文压缩后）

1. **恢复**：读 `docs/DESIGN.md`（重点"设计进度"）+ `adr/` 目录 + `git log`
2. **复述**：AI 先向用户复述项目状态（"上次到阶段 X，下一步是…"），用户确认后再动手
3. **继续**：从"设计进度.下一步"推进，每次只前进一小步
4. **决策**：拿不准的查 ADR，查不到就问人，不猜

## 6. 从设计到开发的交接

设计定稿（阶段 0-6 完成 + 评审通过）后：

1. 加载 `ai-game-dev` 技能（单功能迭代工作流）
2. 按功能清单优先级逐个开发：**F01 → 测试 → 提交 → F02 → …**
3. 每个功能开发前，确认其验收标准在 DESIGN.md 中已写明
4. 开发中发现设计缺陷 → 回到本流程改 DESIGN.md + 写 ADR，不带着 bug 往下走

## 7. 错误示范（不要这样）

| ❌ 错误 | ✅ 正确 |
|--------|--------|
| "帮我做个游戏" → AI 直接开始写代码 | 先阶段 0 问清概念，文档先行 |
| 一次问 15 个问题 | 一次 1~3 个，答完即写文档 |
| 设计靠对话记忆，换会话就丢 | 全部进 DESIGN.md + ADR |
| AI 说"我觉得肉鸽更好"并擅自改玩法 | AI 给选项，人拍板 |
| 一口气实现 10 个功能再测试 | 单功能迭代：一个做完验证再下一个 |
